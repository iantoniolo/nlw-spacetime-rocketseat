generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model User {
  // uuid = Universal Unique ID
  id   String @id @default(uuid())
  name String
}
